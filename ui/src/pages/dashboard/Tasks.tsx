import React from 'react';
import { Header } from '../../components/common';

const Tasks = () => {
    return (
        <section>
            <Header>
                <h1>Tasks</h1>
            </Header>
        </section>
    );
};

export default Tasks;
